<?xml version="1.0" encoding="UTF-8"?>
<!--
    Add opened_by_user_id to m_cashiers so the active cashier session can be
    filtered by the app user who opened it, not only by staff_id (multiple
    app users may share the same staff_id).
-->
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="1" author="fineract">
        <addColumn tableName="m_cashiers">
            <column name="opened_by_user_id" type="BIGINT">
                <constraints nullable="true"/>
            </column>
        </addColumn>
    </changeSet>

    <changeSet id="2" author="fineract">
        <addForeignKeyConstraint baseTableName="m_cashiers" baseColumnNames="opened_by_user_id"
                                 constraintName="FK_m_cashiers_opened_by_user"
                                 referencedTableName="m_appuser" referencedColumnNames="id"
                                 onDelete="SET NULL" onUpdate="RESTRICT"/>
    </changeSet>

</databaseChangeLog>

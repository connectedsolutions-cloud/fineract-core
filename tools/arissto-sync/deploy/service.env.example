ARISSTO_SYNC_IMAGE=registry.example.com/credesal/arissto-sync:VERSION
ARISSTO_SYNC_PROD_CONFIRMATION=REPLACE_WITH_PREFLIGHT_FINGERPRINT
# Keep the smaller workflow selected until the 15-service production acceptance
# gates in deploy/README.md pass, then change this to prod-full-resync.
ARISSTO_SYNC_PROD_WORKFLOW=prod-party-resync
